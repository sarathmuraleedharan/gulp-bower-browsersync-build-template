# gulp-bower-browsersync-build-template

A simple and powerfull template for starting UI web development.

1. npm install
2. bower install
3. gulp

## Gulp tasks

1. gulp serve - For starting server.
2. gulp build - For creating build for production environment.
